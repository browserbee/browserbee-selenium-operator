---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: seleniumgrids.selenium-grid.browserbee.io
spec:
  group: selenium-grid.browserbee.io
  names:
    kind: SeleniumGrid
    listKind: SeleniumGridList
    plural: seleniumgrids
    singular: seleniumgrid
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: SeleniumGrid is the Schema for the seleniumgrids API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SeleniumGridSpec defines the desired state of SeleniumGrid
            properties:
              hub:
                description: Hub defines the Selenium Hub configuration (image, replicas,
                  ports, etc.).
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: SeleniumHubSpec defines the desired state of SeleniumHub
                    properties:
                      gridRef:
                        description: GridRef is a reference to the Selenium Grid configuration.
                        properties:
                          name:
                            type: string
                          namespace:
                            type: string
                        required:
                        - name
                        type: object
                      image:
                        description: |-
                          Image is the container image for the Selenium Hub.
                          e.g., "selenium/hub:4.8.3-20230321"
                        pattern: ^.+:.*$
                        type: string
                      port:
                        default: 4444
                        description: Port on which the Hub listens. Typically 4444
                          for Selenium.
                        format: int32
                        type: integer
                      replicas:
                        default: 1
                        description: |-
                          Replicas indicates how many Hub instances to run in parallel.
                          Typically, you only need 1 Hub replica for Selenium Grid,
                          but you can scale horizontally if your use case supports it.
                        format: int32
                        minimum: 1
                        type: integer
                      serviceType:
                        default: ClusterIP
                        description: |-
                          ServiceType defines how the Hub is exposed inside (or outside) the cluster.
                          Acceptable values can include ClusterIP, NodePort, LoadBalancer, etc.
                        enum:
                        - ClusterIP
                        - NodePort
                        - LoadBalancer
                        type: string
                    required:
                    - image
                    type: object
                  status:
                    description: SeleniumHubStatus defines the observed state of SeleniumHub
                    type: object
                type: object
              nodes:
                description: |-
                  Nodes is a list of node configurations (Chrome, Firefox, etc.).
                  This allows you to define multiple node types in a single CR.
                items:
                  description: SeleniumNode is the Schema for the seleniumnodes API
                  properties:
                    apiVersion:
                      description: |-
                        APIVersion defines the versioned schema of this representation of an object.
                        Servers should convert recognized schemas to the latest internal value, and
                        may reject unrecognized values.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                      type: string
                    kind:
                      description: |-
                        Kind is a string value representing the REST resource this object represents.
                        Servers may infer this from the endpoint the client submits requests to.
                        Cannot be updated.
                        In CamelCase.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    metadata:
                      type: object
                    spec:
                      description: SeleniumNodeSpec defines the desired state of SeleniumNode
                      properties:
                        hubRef:
                          description: GridRef is a reference to the Selenium Grid
                            configuration.
                          properties:
                            name:
                              description: Name is the name of the Selenium Hub.
                              pattern: ^[a-zA-Z0-9-]+$
                              type: string
                            namespace:
                              description: Namespace is the namespace where the Selenium
                                Hub is deployed.
                              pattern: ^[a-zA-Z0-9-]+$
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        image:
                          description: |-
                            Image is the container image for this browser node.
                            e.g., "selenium/node-chrome:4.8.3-20230321"
                          pattern: ^.+:.*$
                          type: string
                        replicas:
                          default: 1
                          description: Replicas is the number of node replicas to
                            run for this browser type.
                          format: int32
                          minimum: 1
                          type: integer
                      required:
                      - image
                      type: object
                    status:
                      description: SeleniumNodeStatus defines the observed state of
                        SeleniumNode
                      type: object
                  type: object
                minItems: 1
                type: array
            required:
            - hub
            type: object
          status:
            description: SeleniumGridStatus defines the observed state of SeleniumGrid
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
